[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "intent-cli"
version = "0.1.0"
description = "Intent CLI"
requires-python = ">=3.9"

[project.scripts]
itt = "intent_cli.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
