# Cursor Setup Helper

Cursor's user rules are currently settings-backed rather than exposed as a
stable global skill directory.

`itt setup cursor` stages the helper rule in `~/.intent/generated/cursor/`
so you can:

- paste it into Cursor user rules
- or copy it into a project at `.cursor/rules/intent.mdc`
