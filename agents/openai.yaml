interface:
  display_name: "Intent CLI"
  short_description: "Record, resume, or sync Intent semantics"
  default_prompt: "Use $intent-cli to record, resume, or explicitly sync this project’s verified Intent semantics."
